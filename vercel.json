{
  "buildCommand": "bash -c 'cd frontend && npm install && ./node_modules/.bin/tsc -b && ./node_modules/.bin/vite build'",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'Skipping root install'",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    }
  ]
}
