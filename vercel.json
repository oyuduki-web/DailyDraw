{
  "buildCommand": "cd frontend && npm install && npx --package=typescript tsc -b && npx vite build",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'Skip'",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    }
  ]
}
