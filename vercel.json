{
  "buildCommand": "cd frontend && npm ci && npx tsc -b && npx vite build",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'Skip'",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    }
  ]
}
